// The same work as locals.cx, but through the globals hash table. The gap
// between the two is the cost of not resolving a name at compile time.
let a = 0;
let b = 1;
let c = 2;
for (let i = 0; i < 5000000; i++) {
  a = b + c;
  b = a - c;
  c = a - b;
}
console.log(a, b, c);
