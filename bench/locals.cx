// Local-variable traffic: reads and writes that resolve to stack slots.
{
  let a = 0;
  let b = 1;
  let c = 2;
  for (let i = 0; i < 5000000; i++) {
    a = b + c;
    b = a - c;
    c = a - b;
  }
  console.log(a, b, c);
}
