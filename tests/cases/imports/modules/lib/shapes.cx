export const UNIT = 1;

export class Shape {
  constructor(name: string) {
    this.name = name;
  }
  area() {
    return 0;
  }
  describe() {
    return `${this.name} area ${this.area()}`;
  }
}

export function scale(n: number, by: number) {
  return n * by;
}
