import { Shape, scale, UNIT } from "./shapes.cx";

export class Rect extends Shape {
  constructor(w: unknown, h: Function) {
    super("rect");
    this.w = w;
    this.h = h;
  }
  area() {
    return scale(this.w, this.h);
  }
}

export const unitRect = new Rect(UNIT, UNIT);
