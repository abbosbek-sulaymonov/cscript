function later(value: unknown, ms: number) {
  return new Promise((resolve: Function) => setTimeout(() => resolve(value), ms));
}
export const ready = await later("loaded", 5);
console.log("slow finished");
